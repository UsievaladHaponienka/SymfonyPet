<div class="grid grid-rows bg-lime-200 rounded">
    <div class="my-2 ml-4">
        <a href="{{ path('app_profile', {'profileId' : user.profile.id}) }}">{{ 'My Profile'}}</a>
    </div>
    <div class="my-2 ml-4">
        <a href="#">{{ 'My Friends' }}</a>
    </div>
    <div class="my-2 ml-4">
        <a href="#">{{ 'My Albums' }}</a>
    </div>
    <div class="my-2 ml-4">
        <a href="#">{{ 'My Groups' }}</a>
    </div>
    <div class="my-2 ml-4">
        <a href="{{ path('app_profile_edit') }}">{{ 'Edit profile' }}</a>
    </div>
</div>