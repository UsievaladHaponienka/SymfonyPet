<div class="leave-group-button-{{ groupId }}">
    <button onclick="leaveGroup('{{ groupId }}')"
            class="bg-rose-700 hover:bg-rose-600 focus:shadow-outline focus:outline-none text-white font-bold my-4 py-2 px-4 rounded">
        {{ 'Leave group' }}
    </button>
</div>