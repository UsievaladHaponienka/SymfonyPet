<button id="like-button-{{ type }}-{{ entityId }}"
        onclick="like({{ entityId }}, '{{ type }}')"
        class="{{ styleClass }} focus:shadow-outline focus:outline-none text-white font-bold mt-4 py-2 px-4 rounded">
    {{ buttonText }}
</button>
