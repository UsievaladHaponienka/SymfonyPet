{% extends 'base.html.twig' %}

{% block title %}
    {% if(app.user.profile.username) %}
        {{ app.user.profile.username }}
    {% else %}
        {{ 'My profile' }}
    {% endif %}
{% endblock %}

{% block body %}
    <div class="grid grid-cols-12 gap-4">
        {% if(app.user) %}
            <div class="col-span-2 col-start-2">
                {% include 'components/profile/navigation.html.twig' %}
            </div>
        {% endif %}
        <div class="col-span-8 bg-indigo-300 rounded ">
            <div class="grid grid-rows">
                <div class="grid grid-cols-4 my-2 mx-4">
                    <div class="col-span-1 flex justify-left">
                        <div>
                            <div>
                                <img src="{{ profile.profileImageUrl }}" alt="{{ 'No profile image' }}">
                            </div>
                            {% if app.user.profile.isFriend(profile.id) %}
                                <div class="text-center bg-indigo-200 m-4 rounded">
                                    {{ 'You are friends' }}
                                </div>
                            {% elseif app.user.profile.isFriendshipRequested(profile.id) %}
                                <div class="text-center bg-indigo-200 m-4 rounded">
                                    {{ 'Friendship request sent' }}
                                </div>
                            {% elseif (app.user.profile.id != profile.id) %}
                                {% include('components/buttons/button-blue.html.twig') with {
                                    'url': path('friendship_request_create', {'profileId': profile.id}),
                                    'title': 'Add to Friends'
                                } %}
                            {% endif %}
                        </div>
                    </div>
                    <div class="col-span-3">
                        <div class="grid grid-rows">
                            {% if(profile.username) %}
                                <div class="text-2xl">
                                    {{ profile.username }}
                                </div>
                            {% else %}
                                <div class="text-2xl">
                                    {{ 'No username' }}
                                </div>
                                {% if (app.user and app.user.profile.id == profile.id) %}
                                    <a class="text-xl text-indigo-500"
                                       href="{{ path('profile_edit') }}">{{ 'You can add username here' }}</a>
                                {% endif %}
                            {% endif %}

                            <div class="text-xl mt-4">
                                {% if(profile.description) %}
                                    {{ profile.description }}
                                {% endif %}
                            </div>
                        </div>
                    </div>
                </div>
                {% if (app.user and app.user.profile.id == profile.id) %}
                    <div class="m-4">
                        {{ form_start(postForm) }}
                        {{ form_end(postForm) }}
                    </div>
                {% endif %}
                <div>
                    {% include('components/post/post-list.html.twig') with {'posts': profile.posts} %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
